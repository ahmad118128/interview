import { styled } from '@mui/material';

export const ImgBoxButtons = styled('img')`
  width: 60px;
  height: 60px;
  object-fit: cover;
  border-radius: 6px;
  cursor: pointer;
  box-sizing: border-box;
`;
