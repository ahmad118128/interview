import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StyledTitleNameWrapper = styled(Box)`
  width: 100%;
  display: flex;
  gap: 0.5rem;
  align-items: center;
`;

export const StyledFormButtonsWrapper = styled(Box)`
  width: 100%;
  display: flex;
  justify-content: center;
  gap: 0.75rem;
  margin-top: 2rem;
`;
