import { styled } from '@mui/material';
import Image from 'next/image';

export const ImgBoxButtons = styled(Image)`
  width: 60px;
  height: 60px;
  object-fit: cover;
  border-radius: 6px;
  cursor: pointer;
  box-sizing: border-box;
`;
